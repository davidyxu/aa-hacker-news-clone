<%= form_for(@comment) do |f| %>
  <%= f.hidden_field :post_id, :value => "#{post_id}" %>
  <%= f.hidden_field :parent_id, :value => "#{parent_id}" %>
  <%=f.text_area :body %>
  <br>
  <%=f.submit "Reply" %>
<% end %>